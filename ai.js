#!/usr/bin/env node
'use strict';

/**
 * ai.js
 * -----
 * Backward-compatible alias. The tool is now called "pocu" (Pocket Unix
 * CLI), but `ai` keeps working for anyone who already linked it. This
 * file just delegates straight to pocu.js.
 */

require('./pocu.js');
